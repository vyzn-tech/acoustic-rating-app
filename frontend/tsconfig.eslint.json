// Special typescript project file, used by eslint only
{
    "extends": "./tsconfig.json",
    "include": [
        // repeated from base config
        "src",
        
        // eslint-only inclusions
        "src/index.js",
        "src/bootstrap.js"
    ]
}